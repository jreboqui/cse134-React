
const localAPI = {
    localStorage: [
        {
            userType: "default",
            userName: "default"
        }
    ],
    all: function() {return this.localStorage}
}

export default localAPI